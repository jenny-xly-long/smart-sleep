package ca.era.stride.controller;

public class LoginController {

    public static void authenticateCredentials(String username, String password){
        // Pseudocode for authentification
        /*String hashedPasswd = hash(password);
        sendToDB(username, password);

        return sessionToken;*/
    }
}
